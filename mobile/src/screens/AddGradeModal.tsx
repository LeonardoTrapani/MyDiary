import React from "react";
import { GradeStackScreenProps } from "../../types";
import { View } from "../components/Themed";

const AddHomeworkModal = ({ navigation }: GradeStackScreenProps<"Add">) => {
  return <View></View>;
};

export default AddHomeworkModal;
