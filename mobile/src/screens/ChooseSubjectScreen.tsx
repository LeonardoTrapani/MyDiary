import React from "react";
import { ScreenProps } from "react-native-screens";
import { AddHomeworkStackParamList, AddHomeworkStackScreenProps, RootStackScreenProps, RootTabScreenProps } from "../../types";
import { View } from "../components/Themed";

const ChooseSubjectScreen = ({ navigation }: AddHomeworkStackScreenProps<'ChooseSubject'>) => {
  return <View></View>
};

export default ChooseSubjectScreen;
